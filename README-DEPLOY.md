Quick deploy notes

- Create a GitHub repo and push this folder.
- Connect to Vercel and import the repo.
- Set environment variables for Sanity/Cloudinary if used.
- Point GoDaddy DNS to Vercel (set A/CNAME or use Vercel nameservers).
